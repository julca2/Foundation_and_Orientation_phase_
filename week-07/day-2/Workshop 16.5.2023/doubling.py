# - Create a variable named `base_number` and assign it the value of `123`
# - Create a function called `double_number()` that doubles its input parameter
#   and returns the doubled value
# - Print the result of `double_number(base_number)`

base_number = 123


def double_number(x):
    return x*2


print(double_number(base_number))
